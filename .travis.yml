language: ruby
rvm:
  - 2.6

os:
  - linux
  - osx

arch:
  - amd64
  - arm64

addons:
  apt:
    packages:
      - libgmp-dev
      - ncompress
    update: true
  homebrew:
    packages:
      - gmp
      - ncompress
    update: true

before_install:
  - gem install bundler

script:
  - scripts/ci_test.sh
