language: ruby
rvm:
  - 2.6

os:
  - linux
  - osx

addons:
  apt:
    packages:
      - libgmp-dev
      - ncompress
    update: true
  homebrew:
    packages:
      - gmp
      - ncompress
    update: true

before_install:
  - gem install bundler

script:
  - scripts/ci_test.sh
